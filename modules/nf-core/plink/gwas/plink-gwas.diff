Changes in component 'nf-core/plink/gwas'
Changes in 'plink/gwas/main.nf':
--- modules/nf-core/plink/gwas/main.nf
+++ modules/nf-core/plink/gwas/main.nf
@@ -1,12 +1,12 @@
 process PLINK_GWAS {
     tag "${meta.id}"
-    label 'process_low'
+    label 'process_medium'
 
 
     conda "${moduleDir}/environment.yml"
-    container "${ workflow.containerEngine in ['singularity', 'apptainer'] && !task.ext.singularity_pull_docker_container ?
-        'https://depot.galaxyproject.org/singularity/plink:1.90b6.21--h031d066_5':
-        'quay.io/biocontainers/plink:1.90b6.21--h031d066_5' }"
+    container "${workflow.containerEngine in ['singularity', 'apptainer'] && !task.ext.singularity_pull_docker_container
+        ? 'https://depot.galaxyproject.org/singularity/plink:1.90b6.21--h031d066_5'
+        : 'quay.io/biocontainers/plink:1.90b6.21--h031d066_5'}"
 
     input:
     tuple val(meta), path(bed), path(bim), path(fam)
@@ -15,7 +15,8 @@
     tuple val(meta4), path(phe)
 
     output:
-    tuple val(meta), path("*.{assoc,qassoc}"), emit: assoc
+    tuple val(meta), path("*.assoc"), emit: assoc, optional: true
+    tuple val(meta), path("*.qassoc"), emit: qassoc, optional: true
     tuple val(meta), path("*.log"), emit: log
     tuple val(meta), path("*.nosex"), emit: nosex, optional: true
     tuple val("${task.process}"), val('plink'), eval("plink --version 2>&1 | sed 's/^PLINK v//;s/ .*//'"), emit: versions_plink, topic: versions

'modules/nf-core/plink/gwas/environment.yml' is unchanged
Changes in 'plink/gwas/meta.yml':
--- modules/nf-core/plink/gwas/meta.yml
+++ modules/nf-core/plink/gwas/meta.yml
@@ -75,10 +75,21 @@
           description: |
             Groovy Map containing sample information
             e.g. [ id:'test', single_end:false ]
-      - "*.{assoc,qassoc}":
+      - "*.assoc":
           type: file
-          description: PLINK GWAS association output file (`.assoc` for case/control traits, `.qassoc` for quantitative traits)
-          pattern: "*.{assoc,qassoc}"
+          description: PLINK GWAS association output file for case/control traits
+          pattern: "*.assoc"
+          ontologies: []
+  qassoc:
+    - - meta:
+          type: map
+          description: |
+            Groovy Map containing sample information
+            e.g. [ id:'test', single_end:false ]
+      - "*.qassoc":
+          type: file
+          description: PLINK GWAS association output file for quantitative traits
+          pattern: "*.qassoc"
           ontologies: []
   log:
     - - meta:

'modules/nf-core/plink/gwas/tests/main.nf.test' is unchanged
'modules/nf-core/plink/gwas/tests/pheno_name.config' is unchanged
'modules/nf-core/plink/gwas/tests/main.nf.test.snap' is unchanged
'modules/nf-core/plink/gwas/tests/nextflow.config' is unchanged
************************************************************
