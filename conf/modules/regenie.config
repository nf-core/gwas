/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    nf-core/gwas Nextflow config file
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    REGENIE association module options
----------------------------------------------------------------------------------------
*/

// REGENIE ASSOCIATION

process {

    // Every Step 1 execution mode reads the same canonical trait and derives binary mode from the
    // immutable analysis metadata. Low-memory temporary predictions remain in the task work
    // directory; the attribution process publishes reused bundles under each consuming analysis.
    withName: 'REGENIE_STEP1|REGENIE_SPLITL0|REGENIE_RUNL0|REGENIE_RUNL1' {
        ext.args   = {
            [
                '--phenoColList PHENO',
                meta.is_binary ? '--bt' : '',
                params.regenie_lowmem ? '--lowmem' : '',
                params.regenie_lowmem ? "--lowmem-prefix ${meta.id}.regenie_lowmem" : '',
            ].join(' ').trim()
        }
        ext.prefix = { "${meta.id}.regenie_step1" }
    }

    // Step 1 may be shared by analyses with identical scientific and cohort-defining inputs. Publish
    // the resulting bundle only after it has been fanned back out to each consuming analysis identity.
    withName: 'ATTRIBUTE_REGENIE_PREDICTIONS' {
        ext.when   = { params.save_association_predictions }
        publishDir = [
            path: { "${params.outdir}/intermediates/association_predictions/regenie/${meta.id}" },
            mode: params.publish_dir_mode,
        ]
    }

    withName: 'REGENIE_STEP2' {
        ext.args   = {
            [
                '--phenoColList PHENO',
                meta.is_binary ? '--bt' : '',
                meta.is_binary && meta.method_options.regenie.firth ? '--firth' : '',
                meta.is_binary && meta.method_options.regenie.firth && meta.method_options.regenie.firth_approx ? '--approx' : '',
                meta.is_binary && meta.method_options.regenie.firth ? "--pThresh ${meta.method_options.regenie.firth_p_threshold}" : '',
                meta.method_options.regenie.min_mac != null ? "--minMAC ${meta.method_options.regenie.min_mac}" : '',
            ].join(' ').trim()
        }
        ext.prefix = { "${meta.id}.regenie" }
        publishDir = [
            path: { "${params.outdir}/association/regenie/${meta.id}" },
            mode: params.publish_dir_mode,
            pattern: '*.regenie.gz',
            saveAs: { filename -> filename.replace('_PHENO.regenie.gz', '.gz') },
        ]
    }
}
