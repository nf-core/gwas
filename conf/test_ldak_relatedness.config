/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    nf-core/gwas Nextflow config file
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    LDAK relatedness matrix route test options
----------------------------------------------------------------------------------------
*/

process {
    resourceLimits = [cpus: 4, memory: '8.GB', time: '1.h']
}

params {
    // Config options
    config_profile_name        = 'LDAK relatedness matrix test profile'
    config_profile_description = 'LDAK matrix identity, reuse and filtering scenarios'

    // Save controls
    // Required while the module selector is re-evaluated in this isolated config fragment.
    publish_dir_mode           = 'copy'
    save_relatedness_matrices  = true
}

// nf-test loads this scenario config after the pipeline config has already resolved its module
// selectors. Re-include the LDAK selector after setting the save control so its publish gate sees
// the scenario value rather than the pipeline default.
includeConfig 'modules/ldak.config'
